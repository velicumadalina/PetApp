@model PetApp.Models.Animal
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
</div>
<div class="container">
    <div class="row col-xs-12 margin0">

        <img src="@Model.Photo" width="300" height="200" />
        <h1>@Model.Name</h1>
        <h1>@Model.Colors["primary"]</h1>
    </div>

</div>